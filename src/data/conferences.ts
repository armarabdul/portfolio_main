import { Conference } from "@/types/conference";

// Placeholder — add conferences as they occur.
// Each conference added here will automatically render in the Conferences section.
export const conferences: Conference[] = [];
