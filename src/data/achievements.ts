import { Achievement } from "@/types/achievement";

// Placeholder — add achievements as they are verified.
// Each achievement added here will automatically render in the Achievements section.
export const achievements: Achievement[] = [];
